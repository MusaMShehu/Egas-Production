import { Outlet } from "react-router-dom";

const OpportunitiesLayout = () => {
  return (
    <div>
      {/* <LearnMore /> */}
      <Outlet />
    </div>
  );
};

export default OpportunitiesLayout;
